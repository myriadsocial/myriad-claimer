apiVersion: v1
kind: Service
metadata:
  name: {{ include "myriad-claimer.fullname" . }}
  labels:
    {{- include "myriad-claimer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "myriad-claimer.selectorLabels" . | nindent 4 }}
